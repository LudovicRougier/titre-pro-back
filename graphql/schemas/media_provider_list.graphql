type MediaProviderList {
    rent: [MediaProvider!]
    buy: [MediaProvider!]
    flatrate: [MediaProvider!]
}

extend type Query {
    mediaProviders: [MediaProvider!] @guard
}

input MediaProviderListInput {
    rent: [MediaProviderInput!]
    buy: [MediaProviderInput!]
    flatrate: [MediaProviderInput!]
}
