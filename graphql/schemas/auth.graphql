extend type Mutation {
  login(email: String!, password: String!): JSON!,
  register(name: String!, email: String!, password: String!, country: String!, age: Int!): JSON!,
  refreshToken: JSON! @guard,
  logout: JSON! @guard
}
